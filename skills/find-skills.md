# Skill 搜索与安装

对应本地 skill：`find-skills`

适用场景：
- 想先看看有没有现成 skill 可用
- 不确定这件事该自己做，还是先去 skill 生态里找
- 希望把“搜 skill”这件事做成固定流程

默认流程：
1. 先查 `skills.sh`
2. 找不到再用 GitHub 深搜兜底
3. 仍然没有，再考虑自己造 skill

输出：
- 候选 skill 清单
- 安装命令
- 为什么推荐或不推荐某个 skill

边界：
- 它不是全网穷尽式搜索引擎，也不保证一定找到最佳 skill
- 重点是把检索和安装流程程序化，而不是替你做最终判断
